import styles from "../styles/{componentTemplate}.module.css";
import styles from "../styles/{componentTemplate}.module.scss";
import styled from "styled-components";
import {ComponentTemplate}Model from "../model/{componentTemplate}";

interface {ComponentTemplate}Props {
    foo: string
}

const {ComponentTemplate}Styled = styled`

`;

export default function {ComponentTemplate}({...props}: {ComponentTemplate}Props) {

    const {foo} = {ComponentTemplate}Model();

    return (
        <div class={styles.{componentTemplate}}>
        </div>
        <{ComponentTemplate}Styled>
        </{ComponentTemplate}Styled>
    );
}